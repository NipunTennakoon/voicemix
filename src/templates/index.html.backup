<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VoiceMix - MP3 Voice Processor</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">
</head>
<body>
    <nav class="navbar navbar-dark bg-primary">
        <div class="container">
            <span class="navbar-brand mb-0 h1">
                <i class="fas fa-music"></i> VoiceMix - MP3 Voice Processor
            </span>
        </div>
    </nav>

    <div class="container mt-5">
        <!-- Header Section -->
        <div class="row mb-4">
            <div class="col-12 text-center">
                <h2>Convert Multiple Voices to Unified Voice</h2>
                <p class="text-muted">Upload an MP3 file with multiple speakers, and we'll convert all voices to match the best one</p>
            </div>
        </div>

        <!-- Main Content -->
        <div class="row">
            <div class="col-lg-8 offset-lg-2">
                
                <!-- Step 1: Upload Section -->
                <div class="card shadow-sm mb-4" id="upload-section">
                    <div class="card-header bg-light">
                        <h5 class="mb-0"><i class="fas fa-upload"></i> Step 1: Upload MP3 File</h5>
                    </div>
                    <div class="card-body">
                        <div class="drop-zone" id="dropZone">
                            <i class="fas fa-cloud-upload-alt fa-3x mb-3 text-primary"></i>
                            <p class="mb-2"><strong>Drag & Drop your MP3 file here</strong></p>
                            <p class="text-muted">or</p>
                            <label for="fileInput" class="btn btn-primary">
                                <i class="fas fa-folder-open"></i> Browse Files
                            </label>
                            <input type="file" id="fileInput" accept=".mp3" style="display: none;">
                        </div>
                        <div id="fileInfo" class="mt-3" style="display: none;">
                            <div class="alert alert-info">
                                <i class="fas fa-file-audio"></i> <strong>Selected:</strong> <span id="fileName"></span>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Step 2: Processing Section -->
                <div class="card shadow-sm mb-4" id="processing-section" style="display: none;">
                    <div class="card-header bg-light">
                        <h5 class="mb-0"><i class="fas fa-cog fa-spin"></i> Step 2: Processing Audio</h5>
                    </div>
                    <div class="card-body">
                        <div class="progress mb-3" style="height: 30px;">
                            <div id="progressBar" class="progress-bar progress-bar-striped progress-bar-animated" 
                                 role="progressbar" style="width: 0%">0%</div>
                        </div>
                        <p id="statusMessage" class="text-center text-muted">Starting processing...</p>
                    </div>
                </div>

                <!-- Step 3: Download Section -->
                <div class="card shadow-sm mb-4" id="download-section" style="display: none;">
                    <div class="card-header bg-success text-white">
                        <h5 class="mb-0"><i class="fas fa-check-circle"></i> Step 3: Download Result</h5>
                    </div>
                    <div class="card-body text-center">
                        <i class="fas fa-check-circle fa-4x text-success mb-3"></i>
                        <h5>Processing Complete!</h5>
                        <p class="text-muted">Your unified voice MP3 file is ready</p>
                        <a href="/download" class="btn btn-success btn-lg" download>
                            <i class="fas fa-download"></i> Download Processed File
                        </a>
                        <button id="resetBtn" class="btn btn-outline-secondary btn-lg ms-2">
                            <i class="fas fa-redo"></i> Process Another File
                        </button>
                    </div>
                </div>

                <!-- Error Section -->
                <div class="card shadow-sm mb-4" id="error-section" style="display: none;">
                    <div class="card-header bg-danger text-white">
                        <h5 class="mb-0"><i class="fas fa-exclamation-circle"></i> Error</h5>
                    </div>
                    <div class="card-body">
                        <p id="errorMessage" class="text-danger"></p>
                        <button id="resetErrorBtn" class="btn btn-primary">
                            <i class="fas fa-redo"></i> Try Again
                        </button>
                    </div>
                </div>

                <!-- Info Section -->
                <div class="card shadow-sm">
                    <div class="card-header bg-light">
                        <h5 class="mb-0"><i class="fas fa-info-circle"></i> How It Works</h5>
                    </div>
                    <div class="card-body">
                        <ol class="mb-0">
                            <li><strong>Speaker Diarization:</strong> Identifies different speakers in your audio</li>
                            <li><strong>Voice Quality Evaluation:</strong> Selects the smoothest and clearest voice</li>
                            <li><strong>Voice Conversion:</strong> Converts all other voices to match the selected voice</li>
                            <li><strong>Export:</strong> Saves the unified audio as an MP3 file</li>
                        </ol>
                    </div>
                </div>

            </div>
        </div>
    </div>

    <footer class="mt-5 py-3 bg-light">
        <div class="container text-center text-muted">
            <p class="mb-0">VoiceMix © 2026 - MP3 Voice Processing Tool</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="{{ url_for('static', filename='js/main.js') }}"></script>
</body>
</html>
